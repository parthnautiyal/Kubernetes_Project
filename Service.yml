kind: Service
apiVersion: v1
metadata:
  name: mrdevops
  labels:
    app: mrdevops
spec:
  ports:
    - port: 9000
      targetPort: 80
      nodePort: 31000
  selector:
    app: mrdevops
  type: LoadBalancer
